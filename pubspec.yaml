name: notes_app_provider
description: Another implementation of simple notes app in Flutter, this time to present proper architecture approach with Provider
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  cupertino_icons: 1.0.5
  flutter:
    sdk: flutter
  intl: 0.17.0

dev_dependencies:
  build_runner: 2.3.2
  dart_code_metrics: 5.0.1
  flutter_lints: 2.0.1
  flutter_test:
    sdk: flutter
  very_good_analysis: 3.1.0

flutter:
  uses-material-design: true
  generate: true